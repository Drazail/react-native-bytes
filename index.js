/* eslint linebreak-style: ["error", "windows"] */
/* eslint-disable no-use-before-define */

import { NativeModules } from 'react-native';

const { RNBytes } = NativeModules;

export default RNBytes;
